apiVersion: v1
kind: Service
metadata:
  name: airflow
  namespace: airflow
spec:
  type: NodePort
  selector:
    app: airflow
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
